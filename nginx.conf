server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # React Router를 위해 경로를 찾지 못하면 index.html로 돌려보냄
        try_files $uri $uri/ /index.html;
    }
}